# My Space 설치 가이드

macOS **Apple Silicon**(M1 이상) 전용입니다.

## 1. 내려받기

[releases](https://github.com/rudaks-han/my-space/releases) 에서 `My.Space-<버전>-darwin-aarch64.dmg`

## 2. 설치

`.dmg` 를 열고 **My Space** 를 **Applications** 로 드래그.

## 3. 첫 실행 전 — 이 명령을 꼭 실행

터미널에 붙여넣고 실행하세요.

```bash
xattr -dr com.apple.quarantine "/Applications/My Space.app"
```

안 하면 **"손상되었기 때문에 열 수 없습니다"** 가 뜨고 실행되지 않습니다.
앱에 문제가 있는 게 아니라, Apple 개발자 서명을 하지 않아 macOS 가 막는 것입니다.

이제 실행하면 됩니다.

## 업데이트

앱이 시작할 때 새 버전을 확인하고 알려줍니다. 위 과정을 다시 할 필요는 없습니다.

---

# 직접 빌드하는 경우

여기부터는 받아서 쓰는 사람이 아니라 **`.dmg` 를 직접 만드는 사람**을 위한 내용입니다.

```bash
./install.sh
```

## 업데이터 서명 키

`src-tauri/tauri.conf.json` 에 업데이터 **공개키**가 박혀 있어서, 개인키 없이 빌드하면
`.app` 을 다 만든 뒤 마지막에 이렇게 죽습니다.

```
A public key has been found, but no private key.
Make sure to set `TAURI_SIGNING_PRIVATE_KEY` environment variable.
```

`./install.sh` 는 `~/.tauri/myspace-updater.key` 가 있으면 알아서 넘겨주고, 없으면
그 빌드에서만 업데이터 산출물(`.sig`)을 끄고 진행합니다 — 로컬 설치에는 `.sig` 가
필요 없습니다. 키를 잃으면 이미 설치된 앱의 업데이트가 끊기니 백업해 두세요.

## "작업이 허용되지 않음" 으로 실패한다면

```
hdiutil: create failed - 작업이 허용되지 않음
failed to bundle project: error running bundle_dmg.sh
```

tauri 는 `.dmg` 를 만들 때 임시 볼륨을 `/Volumes` 에 마운트해 앱을 복사하는데, 그 쓰기가
막힌 것입니다. 읽기·목록은 되고 쓰기만 막혀서 원인이 잘 드러나지 않습니다. 원인은 둘이고,
**둘째는 권한을 줘도 풀리지 않습니다.**

1. **터미널 앱의 TCC 권한** — 이건 권한을 주면 풀립니다.

   > 시스템 설정 → 개인정보 보호 및 보안 → **전체 디스크 접근 권한**
   > → `+` 로 **쓰고 있는 터미널 앱**을 추가하고 켜기
   > → 그 앱을 **완전히 종료 후 다시 실행** (TCC 는 재시작해야 적용됩니다)

2. **보안(DLP)/EndpointSecurity 에이전트의 이동식 볼륨 쓰기 차단** — 사내 PC 에 흔합니다.
   전체 디스크 접근 권한을 줘도 `/Volumes` 쓰기는 계속 막힙니다. 같은 디스크 이미지를
   홈 디렉터리 아래에 마운트하면 쓰기가 되는 것으로 구분할 수 있습니다(= 경로 기준 차단).
   떠 있는 확장은 이렇게 확인합니다.

   ```bash
   systemextensionsctl list        # Endpoint Security 확장 목록
   ```

어느 쪽이든 `./install.sh` 는 **멈추지 않습니다.** 빌드 전에 1초짜리 검사로 이 상태를
판별하고, 막혀 있으면 `.app` 만 빌드한 뒤 마운트가 필요 없는 방식(`hdiutil makehybrid`)으로
`.dmg` 를 직접 굽습니다. 배경 이미지·아이콘 배치 같은 **꾸밈만 빠지고**, Applications 로
드래그해 설치하는 동작·실행 권한·서명은 그대로입니다.

## 꾸밈까지 갖춘 .dmg 가 필요하면

CI 에 맡기면 됩니다. 태그를 밀면 GitHub Actions 가 `.dmg` 를 만들어 릴리스 초안에
올립니다 — CI 러너에는 이 제약이 없습니다.

```bash
git tag v0.0.2 && git push origin v0.0.2
```
